<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:clipToPadding="false">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- کارت ابعاد -->
        <LinearLayout
            android:background="@drawable/bg_card"
            android:padding="12dp"
            android:layout_marginBottom="12dp"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView android:text="ابعاد کرکره" android:textStyle="bold" android:textSize="16sp"/>
            <TextView android:text="ارتفاع (cm)"/>
            <EditText android:id="@+id/inputHeightCm" android:layout_width="match_parent" android:inputType="numberDecimal"/>
            <TextView android:text="عرض (cm)" android:paddingTop="6dp"/>
            <EditText android:id="@+id/inputWidthCm" android:layout_width="match_parent" android:inputType="numberDecimal"/>
            <TextView android:id="@+id/textAreaM2" android:text="مساحت: 0.000 متر مربع" android:paddingTop="6dp"/>
        </LinearLayout>

        <!-- کارت تیغه -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="انتخاب تیغه" android:textStyle="bold"/>
            <Spinner android:id="@+id/spinnerBlade" android:layout_width="match_parent"/>
            <TextView android:id="@+id/textBladeLine" android:text="تیغه — قیمت پایه: - | قیمت کل: -"/>
        </LinearLayout>

        <!-- کارت موتور -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="انتخاب موتور" android:textStyle="bold"/>
            <Spinner android:id="@+id/spinnerMotor" android:layout_width="match_parent"/>
            <TextView android:id="@+id/textMotorLine" android:text="موتور — قیمت: -"/>
        </LinearLayout>

        <!-- کارت شفت -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="انتخاب شفت" android:textStyle="bold"/>
            <Spinner android:id="@+id/spinnerShaft" android:layout_width="match_parent"/>
            <TextView android:id="@+id/textShaftLine" android:text="شفت — قیمت پایه: - | قیمت کل: -"/>
        </LinearLayout>

        <!-- کارت قوطی -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="انتخاب قوطی" android:textStyle="bold"/>
            <CheckBox android:id="@+id/checkboxBoxEnabled" android:text="محاسبه قوطی"/>
            <Spinner android:id="@+id/spinnerBox" android:layout_width="match_parent"/>
            <TextView android:id="@+id/textBoxLine" android:text="قوطی — محاسبه نشده"/>
        </LinearLayout>

        <!-- کارت هزینه‌ها -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="هزینه‌های پایه" android:textStyle="bold"/>
            <TextView android:text="نرخ نصب"/>
            <EditText android:id="@+id/inputInstallPrice" android:inputType="numberDecimal" android:text="1"/>
            <TextView android:text="هزینه نصب محاسبه شده"/>
            <TextView android:id="@+id/textInstallComputed" android:text="0 تومان"/>
            <TextView android:text="قیمت جوشکاری"/>
            <EditText android:id="@+id/inputWeldingPrice" android:inputType="numberDecimal"/>
            <TextView android:text="کرایه حمل"/>
            <EditText android:id="@+id/inputTransportPrice" android:inputType="numberDecimal"/>
        </LinearLayout>

        <!-- کارت گزینه‌های اضافی -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="گزینه‌های اضافی" android:textStyle="bold"/>
            <LinearLayout android:id="@+id/extrasContainer" android:orientation="vertical"/>
        </LinearLayout>

        <!-- کارت ریز محاسبات -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="ریز محاسبات" android:textStyle="bold"/>
            <TextView android:id="@+id/textBreakBlade" android:text="جمع تیغه: -"/>
            <TextView android:id="@+id/textBreakMotor" android:text="موتور: -"/>
            <TextView android:id="@+id/textBreakShaft" android:text="جمع شفت: -"/>
            <TextView android:id="@+id/textBreakBox" android:text="جمع قوطی: -"/>
            <TextView android:id="@+id/textBreakInstall" android:text="نصب: -"/>
            <TextView android:id="@+id/textBreakWelding" android:text="جوشکاری: -"/>
            <TextView android:id="@+id/textBreakTransport" android:text="کرایه حمل: -"/>
            <TextView android:id="@+id/textBreakExtras" android:text="گزینه‌های اضافی: -"/>
        </LinearLayout>

        <!-- کارت قیمت نهایی -->
        <LinearLayout android:background="@drawable/bg_card" android:padding="12dp" android:layout_marginBottom="12dp" android:orientation="vertical">
            <TextView android:text="قیمت نهایی" android:textStyle="bold"/>
            <TextView android:id="@+id/textTotal" android:text="قیمت نهایی: -" android:textSize="18sp"/>
        </LinearLayout>

        <!-- دکمه‌ها -->
        <LinearLayout android:id="@+id/bottomBar" android:orientation="vertical" android:padding="12dp">
            <LinearLayout android:orientation="horizontal">
                <Button android:id="@+id/buttonSaveReport" android:text="ذخیره گزارش" android:layout_width="0dp" android:layout_height="48dp" android:layout_weight="1"/>
                <Button android:id="@+id/buttonBasePrice" android:text="صفحه قیمت پایه" android:layout_width="0dp" android:layout_height="48dp" android:layout_weight="1"/>
            </LinearLayout>
            <Button android:id="@+id/buttonReports" android:text="صفحه گزارشات" android:layout_width="match_parent" android:layout_height="48dp" android:layout_marginTop="8dp" android:layout_marginBottom="8dp"/>
            <Button android:id="@+id/buttonRollDiameter" android:text="صفحه قطر رول" android:layout_width="match_parent" android:layout_height="48dp"/>
        </LinearLayout>

    </LinearLayout>
</ScrollView>
