<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- عرض و ارتفاع -->
        <EditText android:id="@+id/inputWidth" android:hint="عرض کرکره (متر یا سانتی‌متر)" />
        <EditText android:id="@+id/inputHeight" android:hint="ارتفاع کرکره (متر یا سانتی‌متر)" />
        <TextView android:id="@+id/areaResult" android:text="مساحت: ..." />

        <!-- تیغه -->
        <Spinner android:id="@+id/bladeSpinner" />
        <TextView android:id="@+id/bladePrice" android:text="قیمت تیغه: ..." />

        <!-- موتور -->
        <Spinner android:id="@+id/motorSpinner" />
        <TextView android:id="@+id/motorPrice" android:text="قیمت موتور: ..." />

        <!-- شفت -->
        <Spinner android:id="@+id/shaftSpinner" />
        <EditText android:id="@+id/shaftLength" android:hint="متراژ شفت (متر)" />
        <TextView android:id="@+id/shaftPrice" android:text="قیمت شفت: ..." />

        <!-- قوطی -->
        <CheckBox android:id="@+id/boxEnabled" />
        <Spinner android:id="@+id/boxSpinner" />
        <EditText android:id="@+id/boxLength" android:hint="متراژ قوطی (متر)" />
        <TextView android:id="@+id/boxPrice" android:text="قیمت قوطی: ..." />

        <!-- قیمت‌های ثابت -->
        <EditText android:id="@+id/installPrice" android:hint="قیمت نصب" />
        <EditText android:id="@+id/weldPrice" android:hint="قیمت جوشکاری" />
        <EditText android:id="@+id/transportPrice" android:hint="کرایه حمل" />

        <!-- چک‌باکس‌ها -->
        <CheckBox android:id="@+id/lockElectric" android:text="جاقفلی برقی" />
        <CheckBox android:id="@+id/lockManual" android:text="جاقفلی دستی" />
        <CheckBox android:id="@+id/motorCover" android:text="کاور موتور" />

        <!-- قطر رول -->
        <TextView android:id="@+id/rollDiameter" android:text="قطر رول: ..." />

        <!-- قیمت نهایی -->
        <TextView android:id="@+id/finalPrice" android:text="قیمت نهایی: ..." />

        <!-- دکمه‌ها -->
        <Button android:id="@+id/basePriceButton" android:text="صفحه قیمت پایه" />
        <Button android:id="@+id/rollPageButton" android:text="صفحه قطر رول" />
    </LinearLayout>
</ScrollView>
