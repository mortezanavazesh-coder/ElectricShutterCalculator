<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- تیغه -->
        <EditText android:id="@+id/bladeName" android:hint="نام تیغه" />
        <EditText android:id="@+id/bladeThickness" android:hint="ضخامت تیغه (میلیمتر)" />
        <EditText android:id="@+id/bladeHeight" android:hint="ارتفاع تیغه (سانتی‌متر)" />
        <EditText android:id="@+id/bladePrice" android:hint="قیمت پایه تیغه (متر مربع)" />
        <Button android:id="@+id/addBlade" android:text="افزودن تیغه" />

        <!-- موتور -->
        <EditText android:id="@+id/motorName" android:hint="نام موتور" />
        <EditText android:id="@+id/motorPrice" android:hint="قیمت پایه موتور" />
        <Button android:id="@+id/addMotor" android:text="افزودن موتور" />

        <!-- شفت -->
        <EditText android:id="@+id/shaftName" android:hint="نام شفت" />
        <EditText android:id="@+id/shaftDiameter" android:hint="قطر شفت (میلیمتر)" />
        <EditText android:id="@+id/shaftPrice" android:hint="قیمت پایه هر متر" />
        <Button android:id="@+id/addShaft" android:text="افزودن شفت" />

        <!-- قوطی -->
        <EditText android:id="@+id/boxName" android:hint="نام قوطی" />
        <EditText android:id="@+id/boxPrice" android:hint="قیمت پایه هر متر" />
        <Button android:id="@+id/addBox" android:text="افزودن قوطی" />

        <!-- قیمت‌های ثابت -->
        <EditText android:id="@+id/installBasePrice" android:hint="قیمت پایه نصب" />
        <EditText android:id="@+id/weldBasePrice" android:hint="قیمت پایه جوشکاری" />
        <EditText android:id="@+id/transportBasePrice" android:hint="قیمت پایه حمل" />

        <!-- چک‌باکس‌ها -->
        <EditText android:id="@+id/optionName" android:hint="نام گزینه (مثلاً جاقفلی برقی)" />
        <EditText android:id="@+id/optionPrice" android:hint="قیمت گزینه" />
        <Button android:id="@+id/addOption" android:text="افزودن گزینه چک‌باکس" />

        <!-- ذخیره و بازگشت -->
        <Button android:id="@+id/saveBasePrices" android:text="ذخیره سازی در حافظه اپ" />
        <Button android:id="@+id/backToMain" android:text="بازگشت به صفحه اصلی" />
    </LinearLayout>
</ScrollView>
