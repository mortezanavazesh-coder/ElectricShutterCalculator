<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.morteza.shuttercalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElectricShutterCalculator">

        <!-- Main launcher -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Core screens -->
        <activity
            android:name=".BasePriceActivity"
            android:exported="false" />

        <activity
            android:name=".RollCalculatorActivity"
            android:exported="false" />

        <!-- Reports screen implemented with PrefsHelper -->
        <activity
            android:name=".ReportsActivity"
            android:exported="false" />

        <!-- Optional legacy entries (در صورت وجود نگه دارید) -->
        <!-- <activity android:name=".ReportActivity" android:exported="false" /> -->
        <!-- <activity android:name=".ReportListActivity" android:exported="false" /> -->

    </application>
</manifest>
