<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.morteza.shuttercalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElectricShutterCalculator">

        <!-- اکتیویتی اصلی -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- صفحه محاسبه قطر رول -->
        <activity android:name=".RollCalculatorActivity" android:exported="true" />

        <!-- صفحه قیمت پایه -->
        <activity android:name=".BasePriceActivity" android:exported="true" />

        <!-- صفحه قطر رول (اضافی) -->
        <activity
            android:name=".RollDiameterActivity"
            android:exported="false"
            android:label="صفحه قطر رول" />
			
    </application>
</manifest>

